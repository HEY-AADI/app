import { useState } from "react";
import type { ReactNode } from "react";
import { Link, useLocation } from "react-router-dom";
import { AlertTriangle, ArrowRight, BookOpenCheck, CheckCircle2, ChevronRight, ClipboardCheck, Download, FileCheck2, Handshake, LineChart, Map, MessageSquareText, Plus, ShieldCheck, Sparkles, UsersRound } from "lucide-react";
import DashboardShell from "@/components/DashboardShell";
import MetricCard from "@/components/MetricCard";
import OpportunityCard from "@/components/OpportunityCard";
import { alumni, metrics, opportunities, skillDemand, skillReadiness, type Role } from "@/data/mock";

export default function Platform({ role }: { role: Role }) {
  const [activeRole, setActiveRole] = useState<Role>(role);
  const location = useLocation();
  const slug = location.pathname.split("/").pop() ?? "overview";
  return <DashboardShell role={activeRole} onRoleChange={setActiveRole}>
    {activeRole === "student" ? <StudentView slug={slug} /> : <RoleView role={activeRole} slug={slug} />}
  </DashboardShell>;
}

function StudentView({ slug }: { slug: string }) {
  if (slug === "pariksha") return <Pariksha />;
  if (slug === "karma") return <Karma />;
  if (slug === "pramana") return <Pramana />;
  if (slug === "alumni") return <AlumniPanel />;
  if (slug === "mentors") return <MentorsPanel />;
  return <StudentOverview />;
}

function PageIntro({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: ReactNode }) {
  return <div className="flex flex-col justify-between gap-5 border-b border-slate-200 px-4 py-8 sm:px-8 sm:py-10 lg:flex-row lg:items-end">
    <div><p className="font-mono text-xs font-semibold uppercase tracking-[0.17em] text-amber-700" data-testid="workspace-eyebrow">{eyebrow}</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-[#0B3C2A] sm:text-4xl" data-testid="workspace-page-title">{title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-600" data-testid="workspace-page-description">{description}</p></div>
    {action}
  </div>;
}

function StudentOverview() {
  const [showMatch, setShowMatch] = useState(false);
  return <div data-testid="student-overview">
    <PageIntro eyebrow="STUDENT WORKSPACE · ANANYA SHARMA" title="Good morning, Ananya." description="Your next opportunity is closer when your skills, evidence and conversations move together." action={<button className="inline-flex items-center gap-2 rounded-md border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-[#0B3C2A] shadow-sm" data-testid="student-notifications-button"><MessageSquareText size={16} />3 new updates</button>} />
    <div className="space-y-8 px-4 py-8 sm:px-8">
      <section className="grid gap-5 lg:grid-cols-[1.2fr_.8fr]" data-testid="student-readiness-section">
        <div className="rounded-xl bg-[#0B3C2A] p-6 text-white sm:p-8"><div className="flex items-start justify-between gap-5"><div><p className="font-mono text-xs uppercase tracking-[0.16em] text-amber-200">YOUR CAREER READINESS</p><p className="mt-3 font-mono text-6xl font-semibold tracking-tight" data-testid="readiness-score">74<span className="text-3xl text-amber-300">%</span></p><p className="mt-1 font-semibold">Good foundation · 3 clear next steps</p></div><div className="relative flex h-24 w-24 items-center justify-center rounded-full border-[10px] border-amber-400/80 border-r-white/10 text-center"><span className="font-mono text-sm font-bold">74%</span></div></div><div className="mt-8 grid gap-4 sm:grid-cols-2">{skillReadiness.slice(0, 4).map(([label, value]) => <div key={label}><div className="mb-2 flex justify-between text-xs text-emerald-100/80"><span>{label}</span><span className="font-mono">{value}%</span></div><div className="h-1.5 rounded-full bg-white/10"><div className="h-full rounded-full bg-amber-400" style={{ width: `${value}%` }} /></div></div>)}</div><Link to="/app/student/pariksha" className="mt-8 inline-flex items-center gap-2 text-sm font-bold text-amber-200" data-testid="readiness-assessment-link">Open Pariksha assessment <ArrowRight size={16} /></Link></div>
        <div className="rounded-xl border border-amber-200 bg-amber-50 p-6 sm:p-8" data-testid="next-best-action-card"><div className="flex items-center justify-between"><span className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[0.14em] text-amber-800"><Sparkles size={15} /> Next best action</span><span className="rounded-full bg-white px-2 py-1 text-[10px] font-semibold text-amber-800">6 hours</span></div><h2 className="mt-6 text-2xl font-bold tracking-tight text-[#0B3C2A]">Complete GMP Fundamentals</h2><p className="mt-3 text-sm leading-6 text-amber-950/70">Three roles you are interested in require this skill. It could improve your match by an estimated 10–12%.</p><button className="mt-7 inline-flex items-center gap-2 rounded-md bg-[#0B3C2A] px-4 py-2.5 text-sm font-bold text-white hover:bg-[#072B1E]" data-testid="start-gmp-module-button">Start learning <ArrowRight size={16} /></button></div>
      </section>
      <section data-testid="student-recommended-section"><div className="flex items-end justify-between"><div><p className="font-mono text-xs font-semibold uppercase tracking-[0.16em] text-amber-700">YOJANA · FOR YOU</p><h2 className="mt-2 text-xl font-bold text-[#0B3C2A]">Recommended opportunities</h2></div><Link to="/opportunities" className="text-sm font-semibold text-[#0B3C2A]" data-testid="student-see-all-opportunities">See all <ChevronRight size={15} className="inline" /></Link></div><div className="mt-4 grid gap-5 xl:grid-cols-2">{opportunities.slice(0, 2).map((opportunity) => <div key={opportunity.id}><OpportunityCard opportunity={opportunity} compact /><div className="-mt-1 rounded-b-xl border border-t-0 border-slate-200 bg-white px-5 pb-4 pt-2"><button onClick={() => setShowMatch(true)} className="text-xs font-bold text-[#0B3C2A] underline underline-offset-4" data-testid={`view-match-explanation-${opportunity.id}`}>Why this match?</button></div></div>)}</div></section>
      <section className="grid gap-5 lg:grid-cols-3" data-testid="student-progress-grid"><ProgressCard icon={Handshake} eyebrow="KARMA" title="Internship in progress" detail="Week 4 of 8 · Check-in due" href="/app/student/karma" testId="internship-progress-card" /><ProgressCard icon={UsersRound} eyebrow="MENTORSHIP" title="2 mentor conversations" detail="Next session · 18 Mar" href="/app/student/mentors" testId="mentorship-progress-card" /><ProgressCard icon={ShieldCheck} eyebrow="PRAMANA" title="82% profile complete" detail="Add one certification" href="/app/student/pramana" testId="passport-progress-card" /></section>
    </div>
    {showMatch && <Modal title="87% · 7 of 9 skills" eyebrow="WHY THIS MATCH?" onClose={() => setShowMatch(false)} testId="match-explanation-modal"><div className="space-y-3">{["Panchakarma", "Patient communication", "Clinical documentation", "Ayurveda fundamentals", "Hygiene protocols"].map((skill) => <p key={skill} className="flex items-center gap-2 text-sm text-slate-700"><CheckCircle2 size={16} className="text-emerald-600" />{skill}</p>)}<p className="mt-4 border-t border-slate-100 pt-4 text-sm text-amber-900"><Sparkles size={16} className="mr-2 inline text-amber-700" />Missing: GMP fundamentals, quality documentation</p></div><button onClick={() => setShowMatch(false)} className="mt-7 w-full rounded-md bg-[#0B3C2A] py-3 text-sm font-bold text-white" data-testid="match-explanation-done">Got it</button></Modal>}
  </div>;
}

function ProgressCard({ icon: Icon, eyebrow, title, detail, href, testId }: { icon: typeof Handshake; eyebrow: string; title: string; detail: string; href: string; testId: string }) { return <Link to={href} className="rounded-xl border border-slate-200 bg-white p-5 transition-all hover:-translate-y-0.5 hover:border-amber-300" data-testid={testId}><Icon size={19} className="text-amber-700" /><p className="mt-5 font-mono text-[10px] font-semibold uppercase tracking-[0.14em] text-slate-500">{eyebrow}</p><h3 className="mt-2 font-bold text-[#0B3C2A]">{title}</h3><p className="mt-1 text-xs text-slate-500">{detail}</p></Link>; }

function Pariksha() { return <div data-testid="pariksha-page"><PageIntro eyebrow="PARIKSHA · READINESS ASSESSMENT" title="See the shape of your capability." description="A short adaptive preview that combines your confidence, knowledge and evidence. Your result is a starting point—not a label." action={<span className="rounded-full border border-amber-200 bg-amber-50 px-3 py-2 text-xs font-semibold text-amber-800">Demo assessment</span>} /><div className="space-y-8 px-4 py-8 sm:px-8"><div className="grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><div className="rounded-xl border border-slate-200 bg-white p-6" data-testid="assessment-summary-card"><p className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-amber-700">CURRENT RESULT</p><div className="mt-6 flex items-end gap-3"><span className="font-mono text-6xl font-semibold text-[#0B3C2A]">74%</span><span className="mb-2 text-sm text-slate-500">ready to grow</span></div><p className="mt-4 text-sm leading-6 text-slate-600">Your strongest foundation is clinical practice. The largest opportunity is industry readiness.</p><div className="mt-6 rounded-lg bg-emerald-50 p-4 text-sm text-emerald-900"><strong>Confidence note</strong><br />Your verified evidence is stronger than your self-assessment in Panchakarma.</div><button className="mt-6 w-full rounded-md bg-[#0B3C2A] py-3 text-sm font-bold text-white" data-testid="retake-assessment-button">Continue assessment</button></div><div className="rounded-xl border border-slate-200 bg-white p-6" data-testid="assessment-skill-breakdown"><div className="flex items-center justify-between"><div><p className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-amber-700">SKILL PROFILE</p><h2 className="mt-2 text-xl font-bold text-[#0B3C2A]">Your readiness by domain</h2></div><BookOpenCheck className="text-amber-700" /></div><div className="mt-7 space-y-5">{skillReadiness.map(([label, value]) => <div key={label}><div className="mb-2 flex justify-between text-sm"><span className="font-medium text-slate-700">{label}</span><span className="font-mono text-[#0B3C2A]">{value}%</span></div><div className="h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-[#0B3C2A]" style={{ width: `${value}%` }} /></div></div>)}</div></div></div><div className="rounded-xl bg-amber-50 p-6 sm:p-8" data-testid="assessment-next-actions"><p className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-amber-800">RECOMMENDED ACTIONS</p><div className="mt-5 grid gap-4 md:grid-cols-3">{[["Close a gap", "GMP Fundamentals", "6 hours"], ["Show evidence", "Upload clinical portfolio", "15 min"], ["Talk to someone", "Request a QA mentor", "2 slots"]].map(([eyebrow, title, detail]) => <button key={title} className="rounded-lg border border-amber-200 bg-white p-4 text-left hover:border-amber-400" data-testid={`assessment-action-${title.toLowerCase().replaceAll(" ", "-")}`}><p className="text-[10px] font-bold uppercase tracking-wider text-amber-700">{eyebrow}</p><p className="mt-3 text-sm font-bold text-[#0B3C2A]">{title}</p><p className="mt-1 text-xs text-slate-500">{detail}</p></button>)}</div></div></div></div>; }

function Karma() {
  const [evidence, setEvidence] = useState(false);
  const timeline: Array<[string, string, string, boolean]> = [["19 Feb", "Application accepted", "Mentor assigned · complete", true], ["26 Feb", "Deliverable defined", "QA workflow brief · complete", true], ["12 Mar", "Week 4 check-in", "Divergence detected · action needed", false], ["16 Apr", "Final evaluation", "Evidence pack generated after review", false]];
  return <div data-testid="karma-page"><PageIntro eyebrow="KARMA · INTERNSHIP EXPERIENCE" title="Turn work into verified progress." description="Your internship has a mentor, a deliverable and regular moments to make sure the experience is moving in the right direction." action={<span className="inline-flex items-center gap-2 rounded-full bg-emerald-50 px-3 py-2 text-xs font-semibold text-emerald-800"><span className="h-2 w-2 rounded-full bg-emerald-600" />Active · Week 4 of 8</span>} /><div className="space-y-8 px-4 py-8 sm:px-8"><div className="grid gap-5 lg:grid-cols-3"><div className="rounded-xl bg-[#0B3C2A] p-6 text-white lg:col-span-2" data-testid="karma-overview-card"><p className="font-mono text-xs uppercase tracking-[0.14em] text-amber-200">AYURVEDA QUALITY ASSOCIATE</p><h2 className="mt-3 text-2xl font-bold">Arogya Botanicals</h2><p className="mt-1 text-sm text-emerald-100/70">Hybrid · Pune · ₹8,000 / month</p><div className="mt-8 h-2 rounded-full bg-white/10"><div className="h-full w-1/2 rounded-full bg-amber-400" /></div><div className="mt-3 flex justify-between text-xs text-emerald-100/60"><span>Started 19 Feb 2026</span><span>Ends 16 Apr 2026</span></div></div><div className="rounded-xl border border-amber-200 bg-amber-50 p-6" data-testid="karma-alert-card"><div className="flex items-center gap-2 text-amber-800"><AlertTriangle size={18} /><span className="text-xs font-bold uppercase tracking-wider">Attention required</span></div><h3 className="mt-4 font-bold text-[#0B3C2A]">Check-in responses diverged</h3><p className="mt-2 text-sm leading-6 text-amber-950/70">You reported no weekly 1:1. Your mentor reported a weekly meeting.</p><button className="mt-5 text-sm font-bold text-amber-900 underline underline-offset-4" data-testid="review-divergence-button">Review with institution</button></div></div><div className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-xl border border-slate-200 bg-white p-6" data-testid="karma-timeline"><div className="flex items-center justify-between"><div><p className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-amber-700">MILESTONE LOG</p><h2 className="mt-2 text-xl font-bold text-[#0B3C2A]">Experience timeline</h2></div><ClipboardCheck className="text-amber-700" /></div><div className="mt-7 space-y-0">{timeline.map(([date, title, copy, done]) => <div key={title} className="relative flex gap-4 pb-7 last:pb-0"><div className="relative z-10 flex h-7 w-7 shrink-0 items-center justify-center rounded-full border-2 border-white bg-emerald-100 text-emerald-700 shadow-sm">{done ? <CheckCircle2 size={16} /> : <span className="h-2 w-2 rounded-full bg-slate-400" />}</div><div className="-mt-1"><p className="font-mono text-[10px] font-semibold uppercase tracking-wider text-slate-400">{date}</p><p className="mt-1 text-sm font-bold text-[#0B3C2A]">{title}</p><p className="mt-1 text-xs text-slate-500">{copy}</p></div>{title !== "Final evaluation" && <span className="absolute left-3.5 top-7 h-full border-l border-dashed border-slate-200" />}</div>)}</div></div><div className="rounded-xl border border-slate-200 bg-white p-6" data-testid="karma-deliverable-card"><p className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-amber-700">DEFINED DELIVERABLE</p><h2 className="mt-3 text-xl font-bold text-[#0B3C2A]">QA workflow documentation</h2><p className="mt-3 text-sm leading-6 text-slate-600">Create a documented QA workflow for one selected production process.</p><div className="mt-6 rounded-lg bg-slate-50 p-4"><p className="text-xs font-semibold text-slate-500">MENTOR</p><p className="mt-1 text-sm font-bold text-[#0B3C2A]">Dr. Rahul Mehta</p><p className="mt-1 text-xs text-slate-500">Quality & Compliance Lead</p></div><button onClick={() => setEvidence(true)} className="mt-5 flex w-full items-center justify-center gap-2 rounded-md border border-[#0B3C2A] py-3 text-sm font-bold text-[#0B3C2A] hover:bg-emerald-50" data-testid="generate-evidence-pack-button"><FileCheck2 size={16} />Preview evidence pack</button></div></div></div>{evidence && <Modal title="Internship Evidence Pack" eyebrow="PRAMANA · GENERATED PREVIEW" onClose={() => setEvidence(false)} testId="evidence-pack-modal"><div className="space-y-3">{[["01", "Deliverable", "QA workflow documentation"], ["02", "Performance metric", "Reduced process ambiguity · mentor-reviewed"], ["03", "Mentor assessment", "Strong documentation and learning agility"], ["04", "Completion verification", "Pending final evaluation · 16 Apr 2026"]].map(([number, title, copy]) => <div key={number} className="flex gap-3 rounded-lg bg-slate-50 p-3"><span className="font-mono text-xs text-amber-700">{number}</span><div><p className="text-sm font-bold text-[#0B3C2A]">{title}</p><p className="mt-1 text-xs text-slate-500">{copy}</p></div></div>)}</div><div className="mt-6 flex flex-col gap-3 sm:flex-row"><button className="inline-flex flex-1 items-center justify-center gap-2 rounded-md bg-[#0B3C2A] py-3 text-sm font-bold text-white" data-testid="download-evidence-pack-button"><Download size={16} />Download preview</button><button onClick={() => setEvidence(false)} className="rounded-md border border-slate-200 px-5 py-3 text-sm font-bold text-slate-700" data-testid="close-evidence-pack-button">Close</button></div></Modal>}</div>;
}

function Pramana() { return <div data-testid="pramana-page"><PageIntro eyebrow="PRAMANA · CAREER PASSPORT" title="Your proof of progress, in one place." description="A portable career view with evidence and provenance. This prototype simulates credential verification; future DigiLocker and APAAR connections are integration-ready." action={<button className="inline-flex items-center gap-2 rounded-md bg-[#0B3C2A] px-4 py-2.5 text-sm font-bold text-white" data-testid="download-passport-button"><Download size={16} />Download view</button>} /><div className="space-y-8 px-4 py-8 sm:px-8"><div className="grid gap-5 md:grid-cols-4">{[["Verified skills", "12"], ["Verified experience", "3"], ["Projects", "4"], ["Mentor sessions", "7"]].map(([label, value]) => <div key={label} className="rounded-xl border border-slate-200 bg-white p-5"><p className="font-mono text-3xl font-semibold text-[#0B3C2A]">{value}</p><p className="mt-2 text-xs font-medium text-slate-500">{label}</p></div>)}</div><div className="grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><div className="rounded-xl border border-slate-200 bg-white p-6 sm:p-8" data-testid="career-passport-card"><div className="flex items-start justify-between border-b border-slate-200 pb-6"><div><p className="font-mono text-[10px] uppercase tracking-[0.16em] text-amber-700">SAMANVAYA CAREER PASSPORT</p><h2 className="mt-2 text-2xl font-bold text-[#0B3C2A]">Ananya Sharma</h2><p className="mt-1 text-sm text-slate-500">BAMS · Final year · Ayurveda</p></div><div className="flex h-16 w-16 items-center justify-center border-2 border-[#0B3C2A] text-center font-mono text-[9px] font-bold text-[#0B3C2A]">VERIFY<br />DEMO</div></div><div className="mt-6 grid gap-6 sm:grid-cols-2"><div><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Education</p><p className="mt-2 text-sm font-semibold text-[#0B3C2A]">National Institute of Ayurveda</p><p className="mt-1 text-xs text-slate-500">Graduating 2026</p></div><div><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Career interest</p><p className="mt-2 text-sm font-semibold text-[#0B3C2A]">Quality & clinical operations</p><p className="mt-1 text-xs text-slate-500">Open to hybrid opportunities</p></div></div><div className="mt-7 border-t border-slate-100 pt-6"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">Skill provenance</p><div className="mt-4 space-y-4">{[["Panchakarma", 82, "Employer verified"], ["Clinical documentation", 76, "Institution verified"], ["GMP fundamentals", 51, "Self-declared · low confidence"]].map(([skill, value, source]) => <div key={skill}><div className="flex justify-between text-sm"><span className="font-medium text-slate-700">{skill}</span><span className="font-mono text-[#0B3C2A]">{value}%</span></div><div className="mt-2 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-[#0B3C2A]" style={{ width: `${value}%` }} /></div><p className="mt-1 text-[11px] text-slate-500">{source}</p></div>)}</div></div></div><div className="rounded-xl border border-amber-200 bg-amber-50 p-6" data-testid="credential-verification-card"><div className="flex items-center gap-2 text-amber-800"><ShieldCheck size={18} /><p className="text-xs font-bold uppercase tracking-[0.14em]">Credential preview</p></div><h2 className="mt-5 text-xl font-bold text-[#0B3C2A]">Ayurveda Quality Internship</h2><p className="mt-2 text-sm text-amber-950/70">Arogya Botanicals · 19 Feb 2026</p><div className="mt-6 border-y border-amber-200 py-5 text-xs text-amber-950/70"><p><strong className="text-amber-900">Issuer</strong><br />Arogya Botanicals · Demo employer</p><p className="mt-4"><strong className="text-amber-900">Evidence</strong><br />Deliverable · mentor assessment · completion review</p><p className="mt-4 font-mono"><strong className="font-sans text-amber-900">Credential ID</strong><br />SAM-AYU-2026-00412</p></div><button className="mt-6 w-full rounded-md bg-[#D97706] py-3 text-sm font-bold text-white" data-testid="verify-credential-button">Verify credential <ArrowRight size={15} className="ml-1 inline" /></button><p className="mt-3 text-center text-[11px] text-amber-900/60">Simulated verification · production integration-ready</p></div></div></div></div>; }

function AlumniPanel() { return <div data-testid="alumni-panel"><PageIntro eyebrow="ALUMNI NETWORK" title="People who have walked the path." description="Meet AYUSH professionals who are now inside the industry—and are making time to help the next cohort navigate it." action={<Link to="/alumni" className="inline-flex items-center gap-2 rounded-md border border-slate-200 bg-white px-4 py-2.5 text-sm font-bold text-[#0B3C2A]" data-testid="view-full-alumni-link">Browse alumni <ArrowRight size={15} /></Link>} /><div className="space-y-8 px-4 py-8 sm:px-8"><div className="grid gap-5 lg:grid-cols-3">{alumni.map((person) => <div key={person.name} className="rounded-xl border border-slate-200 bg-white p-5" data-testid={`student-alumni-card-${person.initials}`}><div className="flex items-start gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-full bg-emerald-50 font-bold text-emerald-800">{person.initials}</div><div><p className="font-bold text-[#0B3C2A]">{person.name}</p><p className="mt-1 text-xs text-slate-500">{person.role}</p></div></div><div className="mt-5 flex flex-wrap gap-2">{person.focus.map((item) => <span key={item} className="rounded-full bg-slate-50 px-2 py-1 text-[11px] text-slate-600">{item}</span>)}</div><button className="mt-5 w-full rounded-md border border-[#0B3C2A] py-2.5 text-xs font-bold text-[#0B3C2A]" data-testid={`request-mentorship-${person.initials}`}>Request mentorship</button></div>)}</div></div></div>; }

function MentorsPanel() { return <div data-testid="mentors-panel"><PageIntro eyebrow="MENTORSHIP" title="A conversation can change the route." description="Browse alumni and faculty mentors by expertise, then request a focused conversation. No in-app chat—just purposeful connections." /><div className="space-y-8 px-4 py-8 sm:px-8"><div className="grid gap-5 md:grid-cols-2">{alumni.slice(0, 2).map((person) => <div key={person.name} className="flex flex-col justify-between rounded-xl border border-slate-200 bg-white p-6 sm:flex-row sm:items-center" data-testid={`mentor-card-${person.initials}`}><div className="flex items-center gap-4"><div className="flex h-14 w-14 items-center justify-center rounded-full bg-emerald-50 font-bold text-emerald-800">{person.initials}</div><div><p className="font-bold text-[#0B3C2A]">{person.name}</p><p className="mt-1 text-sm text-slate-600">{person.role}</p><p className="mt-2 text-xs text-emerald-700">Available · {person.sessions} sessions hosted</p></div></div><button className="mt-5 rounded-md bg-[#0B3C2A] px-4 py-2.5 text-sm font-bold text-white sm:mt-0" data-testid={`mentor-request-button-${person.initials}`}>Request a conversation</button></div>)}</div></div></div>; }

function RoleView({ role, slug }: { role: Exclude<Role, "student">; slug: string }) {
  const config = {
    employer: { eyebrow: "EMPLOYER CONSOLE", title: "Build the team your work needs.", description: "Define meaningful opportunities, mentor the next generation and see evidence—not just CVs.", stats: metrics.employer },
    alumni: { eyebrow: "ALUMNI / MENTOR", title: "Turn experience into access.", description: "Offer the guidance you wish you had, in a format that respects your time and a student's context.", stats: [["Students guided", "18", "Across 4 sessions"], ["Sessions hosted", "12", "3 upcoming"], ["Mentoring slots", "2", "Available this month"], ["Industry insights", "6", "Shared with cohorts"]] },
    institution: { eyebrow: "SANSTHAN · INSTITUTION", title: "See where your cohort can go next.", description: "Track student readiness, employer demand and internship conversion with an institutional view of the skill graph.", stats: metrics.institution },
    ministry: { eyebrow: "NEETI · MINISTRY INTELLIGENCE", title: "Read the AYUSH labour market.", description: "Aggregate signals for policy decisions: demand, supply, skill gaps, district opportunity and system fairness.", stats: metrics.ministry },
  }[role];
  const baselineChart: Array<[string, number]> = [["Discovery", 84], ["Engagement", 67], ["Progress", 54], ["Verified outcome", 42]];
  const chartData: Array<[string, number]> = role === "institution" ? skillReadiness : role === "ministry" ? skillDemand.map(([name, , value]) => [name, value] as [string, number]) : baselineChart;
  return <div data-testid={`${role}-dashboard`}><PageIntro eyebrow={`${config.eyebrow}${slug !== "overview" ? ` · ${slug.toUpperCase()}` : ""}`} title={config.title} description={config.description} action={<button className="inline-flex items-center gap-2 rounded-md bg-[#0B3C2A] px-4 py-2.5 text-sm font-bold text-white" data-testid={`${role}-primary-action-button`}><Plus size={16} />{role === "employer" ? "Create opportunity" : "Export report"}</button>} /><div className="space-y-8 px-4 py-8 sm:px-8"><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{config.stats.map(([label, value, note], index) => <MetricCard key={label} label={label} value={value} note={note} testId={`${role}-metric-${index}`} />)}</div><div className="grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-xl border border-slate-200 bg-white p-6" data-testid={`${role}-analytics-card`}><div className="flex items-start justify-between"><div><p className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-amber-700">{role === "ministry" ? "NATIONAL DEMAND SIGNAL" : role === "institution" ? "COHORT SKILL READINESS" : role === "employer" ? "HIRING OVERVIEW" : "MENTORING IMPACT"}</p><h2 className="mt-2 text-xl font-bold text-[#0B3C2A]">{role === "ministry" ? "Quality assurance is the strongest gap" : role === "institution" ? "Where students need support" : role === "employer" ? "Your opportunity funnel" : "Practical support, visible outcomes"}</h2></div><LineChart className="text-amber-700" /></div><div className="mt-7 space-y-5">{chartData.map(([label, value]) => <div key={label}><div className="mb-2 flex justify-between text-sm"><span className="font-medium text-slate-700">{label}</span><span className="font-mono text-[#0B3C2A]">{value}%</span></div><div className="h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-[#0B3C2A]" style={{ width: `${value}%` }} /></div></div>)}</div><button className="mt-7 inline-flex items-center gap-2 text-sm font-bold text-[#0B3C2A]" data-testid={`${role}-analytics-detail-button`}>Explore the detail <ArrowRight size={15} /></button></div><div className="rounded-xl border border-slate-200 bg-white p-6" data-testid={`${role}-action-card`}><p className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-amber-700">{role === "ministry" ? "POLICY SIGNAL" : "NEXT ACTION"}</p><h2 className="mt-3 text-xl font-bold text-[#0B3C2A]">{role === "ministry" ? "2 districts need a closer look" : role === "institution" ? "18 students need an industry pathway" : role === "employer" ? "7 candidates ready for review" : "3 students are waiting for your experience"}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{role === "ministry" ? "High graduate supply and low employer density create a priority signal. Data is aggregated demo data." : "Make the next action visible, specific and useful."}</p><button className="mt-7 inline-flex items-center gap-2 rounded-md border border-[#0B3C2A] px-4 py-2.5 text-sm font-bold text-[#0B3C2A]" data-testid={`${role}-action-card-button`}>Review now <ChevronRight size={16} /></button></div></div>{role === "ministry" && <div className="rounded-xl border border-slate-200 bg-white p-6" data-testid="ministry-demand-map"><div className="flex items-center justify-between"><div><p className="font-mono text-xs font-semibold uppercase tracking-[0.14em] text-amber-700">DISTRICT VIEW</p><h2 className="mt-2 text-xl font-bold text-[#0B3C2A]">Demand signals across India</h2></div><Map className="text-amber-700" /></div><div className="mt-6 grid gap-3 sm:grid-cols-3">{[["Pune", "High demand", "Quality & GMP"], ["Bengaluru", "Growing", "Clinical operations"], ["Jaipur", "Priority", "Employer density"]].map(([district, signal, focus]) => <div key={district} className="rounded-lg border border-slate-100 bg-slate-50 p-4"><p className="font-semibold text-[#0B3C2A]">{district}</p><p className="mt-2 text-xs font-semibold text-amber-700">{signal}</p><p className="mt-1 text-xs text-slate-500">{focus}</p></div>)}</div></div>}</div></div>;
}

function Modal({ eyebrow, title, onClose, children, testId }: { eyebrow: string; title: string; onClose: () => void; children: ReactNode; testId: string }) { return <div className="fixed inset-0 z-50 flex items-end justify-center bg-[#072B1E]/40 p-4 sm:items-center" role="dialog" aria-modal="true" aria-label={title} data-testid={testId}><div className="w-full max-w-xl rounded-xl bg-white p-6 shadow-2xl sm:p-8"><div className="flex items-start justify-between"><div><p className="font-mono text-xs font-bold uppercase tracking-[0.14em] text-amber-700">{eyebrow}</p><h2 className="mt-2 text-2xl font-bold text-[#0B3C2A]">{title}</h2></div><button onClick={onClose} className="text-2xl text-slate-400" aria-label={`Close ${title}`} data-testid={`${testId}-close`}>×</button></div><div className="mt-6">{children}</div></div></div>; }