import { Link } from "react-router-dom";
import { ArrowRight, CalendarDays, CheckCircle2, Linkedin, UsersRound } from "lucide-react";
import SiteHeader from "@/components/SiteHeader";
import { alumni, sessions } from "@/data/mock";

export default function Community({ mode }: { mode: "alumni" | "mentors" }) {
  const mentors = alumni.filter((person) => person.available);
  return <div className="min-h-screen bg-[#F8FAF6]"><SiteHeader /><main className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><div className="max-w-3xl"><p className="font-mono text-xs font-semibold uppercase tracking-[0.18em] text-amber-700" data-testid="community-eyebrow">{mode === "alumni" ? "ALUMNI NETWORK" : "MENTORSHIP"}</p><h1 className="mt-3 text-4xl font-bold tracking-tight text-[#0B3C2A] sm:text-5xl" data-testid="community-page-title">{mode === "alumni" ? "People who were once in your position." : "A conversation can change the route."}</h1><p className="mt-4 text-lg leading-8 text-slate-600" data-testid="community-page-description">{mode === "alumni" ? "Meet AYUSH professionals now inside the industry. Browse real pathways, specialisations and the people making time for the next cohort." : "Request focused conversations with alumni and faculty mentors. No noisy social feed—just purposeful guidance."}</p></div><div className="mt-10 grid gap-5 lg:grid-cols-3">{(mode === "alumni" ? alumni : mentors).map((person) => <article key={person.name} className="flex flex-col justify-between rounded-xl border border-slate-200 bg-white p-6 shadow-[0_8px_30px_rgba(15,23,42,.04)]" data-testid={`community-card-${person.initials}`}><div><div className="flex items-start justify-between"><div className="flex h-14 w-14 items-center justify-center rounded-full bg-emerald-50 text-lg font-bold text-emerald-800">{person.initials}</div>{person.available && <span className="inline-flex items-center gap-1 rounded-full bg-emerald-50 px-2 py-1 text-[10px] font-semibold text-emerald-700"><span className="h-1.5 w-1.5 rounded-full bg-emerald-600" />Available</span>}</div><h2 className="mt-5 text-lg font-bold text-[#0B3C2A]" data-testid={`community-name-${person.initials}`}>{person.name}</h2><p className="mt-1 text-sm text-slate-600">{person.role}</p><p className="mt-1 text-xs text-slate-500">{person.education} · {person.organisation}</p><div className="mt-5 flex flex-wrap gap-2">{person.focus.map((focus) => <span key={focus} className="rounded-full border border-emerald-100 bg-emerald-50 px-2.5 py-1 text-[11px] text-emerald-800">{focus}</span>)}</div></div><div className="mt-6 flex items-center gap-3 border-t border-slate-100 pt-5"><button className="flex-1 rounded-md bg-[#0B3C2A] py-2.5 text-xs font-bold text-white" data-testid={`community-request-${person.initials}`}>{mode === "alumni" ? "View profile" : "Request mentorship"}</button>{mode === "alumni" && <span className="text-slate-300" title="LinkedIn profile not added"><Linkedin size={17} aria-label="LinkedIn profile not added" /></span>}</div></article>)}</div><section className="mt-16 rounded-xl bg-[#0B3C2A] p-6 text-white sm:p-8" data-testid="community-sessions-section"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="font-mono text-xs font-semibold uppercase tracking-[0.16em] text-amber-200">UPCOMING ALUMNI SESSIONS</p><h2 className="mt-3 text-2xl font-bold">Practical insight, in a format that fits.</h2></div><span className="inline-flex items-center gap-2 text-xs text-emerald-100/70"><UsersRound size={15} />Open to demo students</span></div><div className="mt-7 grid gap-4 lg:grid-cols-3">{sessions.map((session) => <article key={session.title} className="rounded-lg border border-white/10 bg-white/[.07] p-5" data-testid={`session-card-${session.title.toLowerCase().replaceAll(" ", "-")}`}><p className="text-[10px] font-bold uppercase tracking-[0.14em] text-amber-200">{session.category}</p><h3 className="mt-3 font-bold text-white">{session.title}</h3><p className="mt-2 text-sm text-emerald-100/75">{session.host}</p><p className="mt-3 text-xs text-emerald-100/60">{session.detail}</p><p className="mt-4 inline-flex items-center gap-1 text-xs text-amber-200"><CalendarDays size={14} />{session.interested} students interested</p><button className="mt-5 flex w-full items-center justify-center gap-2 rounded-md border border-white/20 py-2.5 text-xs font-bold text-white hover:bg-white/10" data-testid={`session-register-${session.title.toLowerCase().replaceAll(" ", "-")}`}>Register <ArrowRight size={14} /></button></article>)}</div></section><div className="mt-8 flex flex-col items-start justify-between gap-4 border-t border-slate-200 pt-6 sm:flex-row sm:items-center"><p className="inline-flex items-center gap-2 text-sm text-slate-600"><CheckCircle2 size={17} className="text-emerald-700" />Profiles are demo data; LinkedIn links are shown only when actually added.</p><Link to="/app/student" className="inline-flex items-center gap-2 text-sm font-bold text-[#0B3C2A]" data-testid="community-demo-workspace-link">Open student workspace <ArrowRight size={15} /></Link></div></main></div>;
}