import { useMemo, useState } from "react";
import { Filter, Search, SlidersHorizontal } from "lucide-react";
import SiteHeader from "@/components/SiteHeader";
import OpportunityCard from "@/components/OpportunityCard";
import { opportunities } from "@/data/mock";

export default function Opportunities() {
  const [query, setQuery] = useState("");
  const [system, setSystem] = useState("All systems");
  const filtered = useMemo(() => opportunities.filter((opportunity) => `${opportunity.title} ${opportunity.organisation} ${opportunity.location}`.toLowerCase().includes(query.toLowerCase()) && (system === "All systems" || opportunity.system === system)), [query, system]);
  return <div className="min-h-screen bg-[#F8FAF6]"><SiteHeader /><main className="mx-auto max-w-7xl px-4 py-12 sm:px-6 lg:px-8"><div className="max-w-3xl"><p className="font-mono text-xs font-semibold uppercase tracking-[0.18em] text-amber-700" data-testid="opportunities-eyebrow">YOJANA · OPPORTUNITY FINDER</p><h1 className="mt-3 text-4xl font-bold tracking-tight text-[#0B3C2A] sm:text-5xl" data-testid="opportunities-page-title">Find work that builds your next proof point.</h1><p className="mt-4 text-lg leading-8 text-slate-600" data-testid="opportunities-page-description">Explore internships, projects and early-career roles across the AYUSH ecosystem. Every match explains what fits and what to do next.</p></div><div className="mt-10 flex flex-col gap-3 rounded-xl border border-slate-200 bg-white p-3 shadow-sm sm:flex-row" data-testid="opportunity-filter-bar"><label className="flex min-h-11 flex-1 items-center gap-2 rounded-lg bg-slate-50 px-3 text-slate-500"><Search size={17} /><span className="sr-only">Search opportunities</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search role, organisation or location" className="w-full bg-transparent text-sm text-slate-800 outline-none" data-testid="opportunity-search-input" /></label><label className="flex min-h-11 items-center gap-2 rounded-lg border border-slate-200 px-3 text-sm font-medium text-slate-700"><Filter size={16} className="text-amber-700" /><span className="sr-only">Filter AYUSH system</span><select value={system} onChange={(event) => setSystem(event.target.value)} className="bg-transparent outline-none" data-testid="opportunity-system-filter"><option>All systems</option><option>Ayurveda</option><option>Cross-system</option><option>Siddha</option><option>Unani</option></select></label><button className="inline-flex min-h-11 items-center justify-center gap-2 rounded-lg border border-slate-200 px-4 text-sm font-semibold text-[#0B3C2A] hover:bg-slate-50" data-testid="opportunity-more-filters"><SlidersHorizontal size={16} />More filters</button></div><div className="mt-10 flex items-center justify-between"><div><p className="text-sm font-semibold text-slate-700" data-testid="opportunity-result-count">{filtered.length} opportunities matched to the demo profile</p><p className="mt-1 text-xs text-slate-500">Mock data · skill graph preview</p></div><button className="hidden text-sm font-semibold text-[#0B3C2A] sm:block" data-testid="save-search-button">Save this search</button></div><div className="mt-5 grid gap-5 lg:grid-cols-2">{filtered.map((opportunity) => <OpportunityCard opportunity={opportunity} key={opportunity.id} />)}</div>{filtered.length === 0 && <div className="mt-5 rounded-xl border border-dashed border-slate-300 bg-white p-12 text-center" data-testid="opportunity-empty-state"><p className="font-semibold text-[#0B3C2A]">No opportunities match those filters yet.</p><p className="mt-2 text-sm text-slate-500">Try another AYUSH system or broaden your search.</p></div>}</main></div>;
}